# Reuse the application dependency set by default so local installs match the Docker image.
-r app/requirements.txt
# Added semantic search dependencies to align base install with RAG modules.  # change rationale comment
sentence-transformers>=2.2.2
chromadb>=0.4.0
tiktoken>=0.5.0
